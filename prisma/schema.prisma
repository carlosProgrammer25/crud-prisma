generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model clientes {
  ID    Int    @id @default(autoincrement())
  Nome  String @db.VarChar(150)
  Idade Int
  UF    String @db.Char(2)
}
